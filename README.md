# Simple vicinae Installer for GNOME

*Install command:*
```bash
  sh -c "$(curl -fsSL https://raw.githubusercontent.com/z-Eduard005/gnome-vicinae-installer/main/install.sh)"
```

**You can change hotkeys in default settings app later (saved to custom shortcuts)**